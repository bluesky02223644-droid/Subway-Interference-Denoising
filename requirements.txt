# 深度学习框架
torch>=2.0.0
torchvision>=0.15.0
pytorch-lightning>=2.0.0

# 数据处理
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
h5py>=3.8.0

# 可视化
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.14.0

# 配置管理
pyyaml>=6.0
omegaconf>=2.3.0

# 模型压缩与部署
onnx>=1.14.0
onnxruntime>=1.15.0
tensorrt>=8.6.0  # 可选，用于NVIDIA设备

# 评估指标
scikit-learn>=1.3.0
torchmetrics>=1.0.0

# 进度条
tqdm>=4.65.0

# 日志
tensorboard>=2.13.0
wandb>=0.15.0  # 可选，用于实验跟踪

# 数据增强
albumentations>=1.3.0  # 虽然主要用于图像，但也可用于时序

# 工具
joblib>=1.3.0
